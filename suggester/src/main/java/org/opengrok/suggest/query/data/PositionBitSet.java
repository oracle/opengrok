package org.opengrok.suggest.query.data;

import java.util.BitSet;

public class PositionBitSet extends BitSet implements PositionSet {

    @Override
    public boolean isSet(final int pos) {
        return isSet(pos);
    }

}
